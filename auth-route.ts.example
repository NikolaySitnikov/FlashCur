/**
 * NextAuth.js v5 API Route Handler
 * 
 * Location: app/api/auth/[...nextauth]/route.ts
 * 
 * This is the minimal setup required for NextAuth v5 with Next.js 15 App Router
 */

import { handlers } from '@/auth'

export const { GET, POST } = handlers

/**
 * Optional: Specify runtime if using database adapter
 * Some adapters (like Prisma) need Node.js runtime
 */
export const runtime = 'nodejs'
