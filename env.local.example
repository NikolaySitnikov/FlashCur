# ============================================
# NextAuth Configuration (REQUIRED)
# ============================================
# Generate NEXTAUTH_SECRET using:
# macOS/Linux: openssl rand -hex 32
# Windows: [System.Convert]::ToHexString([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-change-this-in-production

# ============================================
# API Configuration
# ============================================
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_WS_URL=ws://localhost:3001

# ============================================
# Stripe Configuration (if using payments)
# ============================================
# Get from https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
# Keep this secret, store in server-only environment
STRIPE_SECRET_KEY=sk_test_your_key_here

# ============================================
# WalletConnect Configuration (IMPORTANT)
# ============================================
# Get your Project ID from https://cloud.walletconnect.com (free tier available)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_actual_walletconnect_project_id_here

# ============================================
# Database Configuration (if using)
# ============================================
# DATABASE_URL=postgresql://user:password@localhost:5432/volspike

# ============================================
# Deployment Notes
# ============================================
# For production (e.g., Vercel):
# 1. Update NEXTAUTH_URL to your production domain
# 2. Generate a new NEXTAUTH_SECRET for production
# 3. Update NEXT_PUBLIC_API_URL to your production API
# 4. Get production WalletConnect Project ID
# 5. Set all environment variables in your hosting platform's dashboard
# 6. NEVER commit .env.local to version control
